/*! Bootstrap 5.3.0 - Placeholder for offline use */
console.log("Bootstrap JS placeholder loaded - replace with actual Bootstrap for offline use");

// Minimal Bootstrap-like functionality
(function() {
    // Basic dropdown toggle
    document.addEventListener('click', function(e) {
        if (e.target.matches('.dropdown-toggle')) {
            e.preventDefault();
            const dropdown = e.target.nextElementSibling;
            if (dropdown && dropdown.classList.contains('dropdown-menu')) {
                dropdown.style.display = dropdown.style.display === 'block' ? 'none' : 'block';
            }
        }
    });

    // Basic modal functionality
    window.bootstrap = {
        Modal: function(element) {
            return {
                show: function() { element.style.display = 'block'; },
                hide: function() { element.style.display = 'none'; }
            };
        }
    };
})();
