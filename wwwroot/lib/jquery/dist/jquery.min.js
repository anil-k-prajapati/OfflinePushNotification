/*! jQuery v3.7.1 - Minimal placeholder for offline use */
(function(window) {
    'use strict';
    
    function jQuery(selector) {
        if (typeof selector === 'string') {
            return new jQuery.fn.init(selector);
        } else if (typeof selector === 'function') {
            // Document ready
            if (document.readyState === 'loading') {
                document.addEventListener('DOMContentLoaded', selector);
            } else {
                selector();
            }
            return this;
        }
        return this;
    }
    
    jQuery.fn = jQuery.prototype = {
        init: function(selector) {
            this.elements = document.querySelectorAll(selector);
            this.length = this.elements.length;
            return this;
        },
        
        on: function(event, handler) {
            for (let i = 0; i < this.elements.length; i++) {
                this.elements[i].addEventListener(event, handler);
            }
            return this;
        },
        
        val: function(value) {
            if (value === undefined) {
                return this.elements[0] ? this.elements[0].value : '';
            }
            for (let i = 0; i < this.elements.length; i++) {
                this.elements[i].value = value;
            }
            return this;
        },
        
        html: function(html) {
            if (html === undefined) {
                return this.elements[0] ? this.elements[0].innerHTML : '';
            }
            for (let i = 0; i < this.elements.length; i++) {
                this.elements[i].innerHTML = html;
            }
            return this;
        },
        
        addClass: function(className) {
            for (let i = 0; i < this.elements.length; i++) {
                this.elements[i].classList.add(className);
            }
            return this;
        },
        
        removeClass: function(className) {
            for (let i = 0; i < this.elements.length; i++) {
                this.elements[i].classList.remove(className);
            }
            return this;
        }
    };
    
    jQuery.fn.init.prototype = jQuery.fn;
    
    // Basic AJAX functionality
    jQuery.ajax = function(options) {
        return fetch(options.url, {
            method: options.method || options.type || 'GET',
            headers: options.headers || {},
            body: options.data
        }).then(response => {
            if (options.dataType === 'json') {
                return response.json();
            }
            return response.text();
        }).then(data => {
            if (options.success) options.success(data);
        }).catch(error => {
            if (options.error) options.error(error);
        });
    };
    
    window.$ = window.jQuery = jQuery;
    
    console.log("jQuery minimal placeholder loaded - replace with full jQuery for complete functionality");
    
})(window);
